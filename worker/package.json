{
  "name": "newsun-atendimento-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API segura do atendente público NewSun em Cloudflare Workers",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote",
    "types": "wrangler types",
    "check": "node --check src/index.js && node --check src/llm.js && node --check src/rag.js && node --check src/handoff.js && node --check src/session.js && node --check src/integrations.js",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
